/**
 * Sort Options Enum
 *
 * Changes:
 * - Created shared enum for sorting options to be used across DTOs
 */

export enum SortByEnum {
  RATING = 'rating',
  DISTANCE = 'distance',
  PROMINENCE = 'prominence',
}
